@page "/"
@namespace Theatre_TimeLine.Pages
@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers
@{
    Layout = "_Layout";
}

@* Using Server render mode instead of ServerPrerendered to avoid MudBlazor provider conflicts
   that can occur during prerendering when authentication state changes *@
<component type="typeof(App)" render-mode="Server" />
